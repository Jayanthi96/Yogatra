# Yogatra
A real-time yoga pose correction system that compares live webcam input against a reference video using MediaPipe and OpenCV. Computes body joint angles frame-by-frame and classifies poses as correct or incorrect with configurable deviation thresholds of ±10°, ±20°, and ±30°.
